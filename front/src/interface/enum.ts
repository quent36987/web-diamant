export enum EAction {
    STAY = "stay",
    LEAVE = "leave",
}

export enum RoundType {
    START = "start",
    CARD = "card",
    END = "end",
}

export enum ECardType {
    DANGER = "danger",
    SPLITER = "spliter",
    FIRSTAID = "firstaid",
}
